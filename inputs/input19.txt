struct Point {
    x: i32;
    y: i32;
}

fn make_point(a: i32, b: i32) -> Point {
    return Point { x: a, y: b };
}

fn main() {
    let p: Point = make_point(1, 2);
    println!("{}", p.y);
}

